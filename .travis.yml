sudo: required


services:
  - docker

before_install:
  - docker build . -t adobe/luamock

script:
  - ./run_unit_tests.sh

deploy:
  skip_cleanup: true
  provider: script
    script: bash deploy.sh snapshot
    except:
      master
  provider: script
    script: bash deploy.sh production
    on:
      branch: master

